export enum  REPORTER {
    QA = "QA",
    DEV = "DEV",
    PO = "PO"
}

export enum PRIORITY {
    MINOR = "Minor",
    MAJOR = "Major",
    CRITICAL = "Critical"
}

export enum STATUS {
    FOR_REVIEW = "For Review",
    DONE = "Done",
    REJECTED = "Rejected"
}